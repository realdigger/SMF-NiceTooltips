<?xml version="1.0"?>
<!DOCTYPE modification SYSTEM "http://www.simplemachines.org/xml/modification">
<!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
	This is an example modification file for SMF packages.

	ATTENTION: If you are trying to install this manually, you should try
	the package manager.  If it will not work for you, please take a look
	at the following for information on this format:
		http://mods.simplemachines.org/docs/manual-install.php

================================================================================

	Modification files can be used to modify files so that they do what
	your package needs them to do to work properly.

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->

<modification xmlns="http://www.simplemachines.org/xml/modification" xmlns:smf="http://www.simplemachines.org/">
	<id>digger:NiceTooltips</id>
	<version>1.9</version>

<!-- Main code -->

	<file name="$sourcedir/MessageIndex.php">

		<operation>

			<search position="replace"><![CDATA[SUBSTRING(mf.body, 1, 385) AS first_body]]></search>

			<add><![CDATA[SUBSTRING(mf.body, 1, ' . (!empty($modSettings['NiceTooltips_lenght']) ? $modSettings['NiceTooltips_lenght'] : 385) . ') AS first_body]]></add>
		</operation>

    <operation>

			<search position="after"><![CDATA[			if (!empty($settings['message_index_preview']))]]></search>

			<add><![CDATA[      // * NiceTooltips mod
	  require_once($sourcedir . '/Mod-NiceTooltips.php');
      $row['nice_tooltip_first_msg'] = NiceTooltip($row['first_body'], $row['first_subject'], $row['first_smileys'], $row['id_first_msg']);

]]></add>

		</operation>
        
		<operation>

			<search position="replace"><![CDATA[					'link' => '<a href="' . $scripturl . '?topic=' . $row['id_topic'] . '.0">' . $row['first_subject'] . '</a>']]></search>

			<add><![CDATA[					// * NiceTooltips mod
					'link' => '<a href="' . $scripturl . '?topic=' . $row['id_topic'] . '.0"'  . $row['nice_tooltip_first_msg'] . ' >' . $row['first_subject'] . '</a>'
]]></add>

		</operation>

	</file>
 		
  <file name="$sourcedir/Recent.php">

    <operation>

			<search position="replace"><![CDATA[SUBSTRING(ml.body, 1, 385) AS last_body]]></search>

			<add><![CDATA[SUBSTRING(ml.body, 1, ' . (!empty($modSettings['NiceTooltips_lenght']) ? $modSettings['NiceTooltips_lenght'] : 385) . ') AS last_body]]></add>
		</operation>

    <operation>

			<search position="after"><![CDATA[		if (!empty($settings['message_index_preview']))]]></search>

			<add><![CDATA[     // * NiceTooltips mod
	 require_once($sourcedir . '/Mod-NiceTooltips.php');
     $row['nice_tooltip_first_msg'] = NiceTooltip($row['last_body'], $row['last_subject'], $row['last_smileys'], $row['id_last_msg']);

]]></add>

		</operation>

		<operation>

			<search position="replace"><![CDATA[				'link' => '<a href="' . $scripturl . '?topic=' . $row['id_topic'] . '.0;topicseen">' . $row['first_subject'] . '</a>']]></search>

			<add><![CDATA[    // * NiceTooltips mod
    'link' => '<a href="' . $scripturl . '?topic=' . $row['id_topic'] . '.0"'  . $row['nice_tooltip_first_msg'] . ' >' . $row['first_subject'] . '</a>'
]]></add>
		</operation>

	</file>

<!-- Admin settings -->
 		
	<file name="$sourcedir/ManageSettings.php">

		<operation>

			<search position="before"><![CDATA[		// Mod authors, add any settings UNDER this line. Include a comma at the end of the line and don't remove this statement!!]]></search>

			<add><![CDATA[
		// * NiceTooltips mod
		'',
		array('int', 'NiceTooltips_lenght'),
		array('check', 'NiceTooltips_censored'),
		array('check', 'NiceTooltips_mobile'),
		array('text', 'NiceTooltips_FGCOLOR'),
		array('text', 'NiceTooltips_BGCOLOR'),
		array('text', 'NiceTooltips_TEXTCOLOR'),
		array('text', 'NiceTooltips_TEXTSIZE'),
		array('text', 'NiceTooltips_CAPCOLOR'),
		array('text', 'NiceTooltips_OPACITY'),
		array('int', 'NiceTooltips_DELAY'),
		array(
            'permissions',
            'disable_nicetooltips',
            'label' => $txt['permissionname_disable_nicetooltips'],
            'subtext' => $txt['permissionhelp_disable_nicetooltips']
        ),
		// NiceTooltips mod *
]]></add>

    </operation>
    
	</file>

	<file name="$sourcedir/ManagePermissions.php">

		<operation>

			<search position="after"><![CDATA[	// All permission groups that will be shown in the left column on classic view.]]></search>

			<add><![CDATA[
	// * NiceTooltips mod
	$permissionList['membergroup']['disable_nicetooltips'] = array(false, 'general', 'moderate_general');
	$permissionList['board']['disable_nicetooltips'] = array(false, 'general_board', 'moderate');
	// NiceTooltips mod *

]]></add>

		</operation>

	</file>

</modification>